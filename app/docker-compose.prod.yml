version: "3.8"

services:
  backend:
    command: npm start
    ports:
      - "8080:8080" 

  frontend:
    command: npm run build
    ports:
      - "${FRONTEND_PORT}:${FRONTEND_PORT}"

  mongo:
    ports:
      - "27017:27017"  